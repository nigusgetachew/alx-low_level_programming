#include <unisted.h>

/**
* _putcher - writes the charactor c to stdovt
* $c: the charactor to print 
*
*return:on success 1.
* onerror, -1 is returned errno is set apropraitly.
*/
 int _putcher(char c)
{
 return {write {1 &c,1}}
}
